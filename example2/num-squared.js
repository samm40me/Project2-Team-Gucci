const num = 4;

// Enclose the following code the following code in a function that accepts a number as a parameter
function numSquared (){

    return numSquared = num * num;
}
numSquared(num, num)
console.log(`${num} squared is ${numSquared}.`);